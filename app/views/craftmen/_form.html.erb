<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>

  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :phone %>
  <%= f.input :photo, as: :attachinary %>

  <%= f.button :submit %>
<% end %>
